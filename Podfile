# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'HDDemo' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for HDDemo
  pod 'HBNetWorking', :path => 'Lib/HBNetWorking'#这个是以为Podfile文件基点找到HBNetWorking所在文件相对路径
  pod 'HBLogin', :path => 'Lib/HBLogin'
end
